ARG APP_CODE_WORKDIR=${APP_CODE_WORKDIR}
ARG NODE_VERSION=${NODE_VERSION}
ARG APP_PORT=${APP_PORT}

FROM node:${NODE_VERSION}-alpine

RUN apk update && \
    apk add git

WORKDIR ${APP_CODE_WORKDIR}

EXPOSE ${APP_PORT}
